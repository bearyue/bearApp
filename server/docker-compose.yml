services:
  postgres:
    image: postgres:16-alpine
    container_name: bear-asset-db
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: bear
      POSTGRES_PASSWORD: bearapp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bear -d postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: bear-asset-server
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/postgres
      SPRING_DATASOURCE_USERNAME: bear
      SPRING_DATASOURCE_PASSWORD: bearapp
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
